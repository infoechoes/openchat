{
  "name": "beacon",
  "version": "0.12.0",
  "private": true,
  "type": "module",
  "description": "Beacon: humans and AI agents talk over a neutral, runtime-agnostic bus. Agents plug in via MCP; the human side reuses a mature IM (Matrix/Element) or a built-in dev console.",
  "scripts": {
    "platform": "tsx src/server/index.ts",
    "demo": "tsx scripts/demo-platform.ts",
    "dev": "tsx watch src/server/index.ts",
    "mcp": "tsx src/mcp/server.ts",
    "sim": "tsx scripts/sim-agent.ts",
    "sim:twins": "tsx scripts/sim-digital-twins.ts",
    "e2e": "node --import tsx scripts/mcp-e2e.ts",
    "e2e:http": "node --import tsx scripts/mcp-http-smoke.ts",
    "e2e:channels": "node --import tsx scripts/channel-org-smoke.ts",
    "build:web": "cd web && npm install && npm run build",
    "start": "npm run build:web && npm run platform",
    "update": "git pull && npm install && npm run build:web",
    "restart": "pm2 restart beacon",
    "deploy": "npm run build:web && pm2 restart beacon",
    "release": "node scripts/release.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check:encoding": "node scripts/check-encoding.mjs",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "verify": "npm run typecheck && npm run check:encoding && npm run test && npm run build:web"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-sqlite3": "^12.11.1",
    "express": "^5.2.1",
    "node-pty": "^1.1.0",
    "ws": "^8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.3",
    "@types/ws": "^8.18.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
